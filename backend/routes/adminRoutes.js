const express=require("express"),r=express.Router(),c=require("../controller/adminController"),{auth,authorizeRoles}=require("../middleware/auth");
r.get("/dashboard",auth,authorizeRoles("admin"),c.dashboard);r.get("/requests",auth,authorizeRoles("admin"),c.requests);r.get("/donors",auth,authorizeRoles("admin"),c.donors);r.post("/inventory",auth,authorizeRoles("admin"),c.updateInventory);r.post("/requests/:id/approve",auth,authorizeRoles("admin"),c.approveRequest);r.post("/requests/:id/reject",auth,authorizeRoles("admin"),c.rejectRequest);r.post("/requests/:id/fulfill",auth,authorizeRoles("admin"),c.fulfillRequest);module.exports=r;
